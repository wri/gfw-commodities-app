define([
	"react"
], function (React) {

	return React.createClass({

    // getInitialState: function () {
    //   return ({
    //     active: this.props.active || false
    //   });
    // },

    componentDidMount: function () {

    },

    render: function () {
      return (
        React.DOM.div({}, "Step Three")
      );
    }

  });

});